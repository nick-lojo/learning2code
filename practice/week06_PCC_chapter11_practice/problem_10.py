# A function checks whether a submitted claim amount is valid.
# It returns True if the amount is positive, False if zero
# or negative.
#
# Write the function in problem_10.py.
# Write a test file with two tests:
#   - one that asserts the return value is True
#   - one that asserts the return value is False
#
# Do not use == in either assert statement.
# Paste both files and output.